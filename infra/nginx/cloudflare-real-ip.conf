# This file is mounted only by compose.tunnel.yaml.
# The static addresses belong exclusively to the two cloudflared services.
set_real_ip_from 172.30.77.3;
set_real_ip_from 172.30.77.4;
real_ip_header CF-Connecting-IP;
real_ip_recursive off;
